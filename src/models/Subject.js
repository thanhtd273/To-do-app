class Subject {
  constructor(id, subject, icon, iconColor, tasks) {
    this.id = id;
    this.subject = subject;
    this.icon = icon;
    this.iconColor = iconColor;
    this.tasks = tasks;
  }
}

export default Subject;
