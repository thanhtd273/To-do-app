class Task {
  constructor(id, title, deadline, isCompleted, reminders) {
    this.id = id;
    this.title = title;
    this.deadline = deadline;
    this.isCompleted = isCompleted;
    this.reminders = reminders;
  }
}

export default Task;
